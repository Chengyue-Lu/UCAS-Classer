<!-- markdownlint-disable -->

## UCAS Classer 使用说明

### 一、首次使用
1. 可先在应用设置中填写 SEP 用户名；它会以明文保存在本地设置中，用于登录页自动填充。
2. 首次需要登录时，程序会自动打开浏览器进入 SEP 登录页；密码和验证码由用户填写。
3. 登录完成后，停留在已登录页面即可，程序会自动保存登录态。
4. 应用启动后会自动执行一次 Check；登录态有效时，会继续执行一次完整 Collect。
5. 首轮完整采集完成后，后续后台自动采集默认走轻量 `summary` 模式；当检测到课程摘要变化时，下一次自动采集会自动升级为完整 `full` 模式。

### 二、主界面按钮
1. Check：检查当前登录态是否仍然有效；若当前处于 interrupt，仍可用来探测联网与登录态是否已恢复，恢复成功后会自动清除 interrupt。
2. Collect：立即执行一次完整采集，并自动导入本地数据库。
3. Login：重新进入登录流程，适用于登录态失效或需要切换账号。

### 三、设置说明
1. SEP 用户名用于登录页自动填充，密码不写入应用设置。
2. 下载目录支持直接手动填写，也支持系统文件夹选择器。
3. 支持课程分目录：可为每门课单独指定主下载目录下的子文件夹。
4. 课程分目录弹窗支持按：全部、当前学期、以往学期过滤显示。
5. 课程范围支持切换为：全部、当前学期、以往学期。
6. Check / Collect / Cookie 刷新时间支持自定义。
7. 自动侧收默认关闭；开启后，窗口拖到屏幕左侧或右侧边缘会自动收起为边缘短条。
8. 自动侧收开启后，侧边收起和侧边展开态会保持置顶；拖离边缘后自动取消置顶。
9. 正常与侧边展开状态的窗口尺寸限制为 480×720 至 720×960；侧收短条尺寸固定，不能拖动缩放。

### 四、下载说明
1. 单个资料或附件可直接下载到本地。
2. 资料模块支持批量下载。
3. 批量下载会保留资料树中的子文件夹层级。
4. 若设置了课程分目录，实际下载位置为：
   主下载目录 / 课程子目录 / 资料原有子路径 / 文件
5. 主界面会显示当前下载状态：`Waiting / Downloading / Success / Fail`。

### 五、详情显示
1. 通知与资料仍可打开原始页面。
2. 作业详情已支持应用内嵌显示。
3. 有详情页面的作业会在点开时按需抓取，并缓存到本地，后续再次打开会优先读取缓存。
4. 如作业详情抓取失败，可在确认登录态有效后重新打开该作业重试。

### 六、提醒说明
1. 成功完成完整导库后，程序会检查是否出现新的通知、资料和作业。
2. 系统提醒按课程聚合显示，不会把同一门课拆成多条细碎弹窗。
3. 首次建立提醒基线时不会提醒历史存量。

### 七、后台与托盘
1. 关闭窗口后，程序默认继续驻留在系统托盘，不会直接退出。
2. 可通过托盘菜单重新打开主窗口或退出程序。
3. 最小化后也可以通过任务栏图标恢复窗口。

### 八、存储位置
1. 打包版的登录态、设置、数据库：
   `%APPDATA%\UCAS Classer`
2. 打包版采集缓存：
   `%LOCALAPPDATA%\UCAS Classer\cache`
3. 开发端会使用仓库内的 `data/` 目录作为本地数据根。

### 九、如果出现异常
1. 先点击 Check，确认登录态是否有效。
2. 若当前只是暂时断网进入 interrupt，可在网络恢复后再次点击 Check；若返回在线，interrupt 会自动解除。
3. 如 Check 失败，再点击 Login 重新登录。
4. 如 Collect 后仍未刷新，等待当前任务完成后再手动执行一次 Collect。
5. 如下载失败，先检查下载目录是否可写，再确认登录态是否仍然有效。
6. 如作业详情加载失败，优先检查登录态与当前课程作业页是否仍可正常访问。

<!-- markdownlint-enable -->
